-- Comment out packer for now and use lazy.nvim instead
-- require("ron.plugins-setup")
require("ron.lazy-setup")  -- New lazy.nvim setup (handles colorscheme)
require("ron.core.options")
require("ron.core.keymaps")
-- require("ron.core.colorscheme") -- Commented out - now handled by lazy.nvim
