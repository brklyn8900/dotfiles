set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @continuum-restore 'on'
set -g @resurrect-capture-pane-contents 'on'

set -g default-terminal "${TERM}"
set -sg terminal-overrides ",*:RGB"

# status bar
set -g status-position top
set -g status-interval 5
set -g status-style bg='#1e1e2e',fg='#6c7086'
set -g status-left "#{?client_prefix,#[fg=#f38ba8]PREFIX #[default],}#{?pane_synchronized,#[fg=#f9e2af]SYNC #[default],}"
set -g status-right "#[fg=#89b4fa]#S"

# window buttons
# set -g window-status-format "●"
# set -g window-status-current-format "●"
set -g monitor-activity on
set -g window-status-format " #W#{?window_flags,#{window_flags},} "
set -g window-status-current-format " #W#{?window_flags,#{window_flags},} "
set -g window-status-style "fg=#6c7086,bg=#1e1e2e"
set -g window-status-activity-style "fg=#a6adc8,bg=#1e1e2e"

set -g window-status-current-style \
      "#{?window_zoomed_flag,fg=#f9e2af,bg=#1e1e2e,bold,fg=#cdd6f4,bg=#1e1e2e,nobold}"

set -g window-status-bell-style "fg=#f38ba8,bg=#1e1e2e,bold"

set -g renumber-windows on

# pane navigation (Opt+hjkl — Ctrl+hjkl is used by Ghostty splits)
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

# pane separators
set -g pane-border-lines simple
set -g pane-border-style fg='#181825'
set -g pane-active-border-style fg='#6c7086'
set -g message-style "fg=#cdd6f4,bg=#313244"
set -g mode-style "fg=#89b4fa,bg=#313244"
set -g popup-style "fg=#cdd6f4,bg=#1e1e2e"
set -g popup-border-style "fg=#45475a"

set -g escape-time 0
set -g repeat-time 1000
set -g history-limit 50000

unbind C-b
set -g prefix '`'
bind-key '`' send-prefix

unbind %
bind | split-window -h -c "#{pane_current_path}"
bind '\' split-window -h -c "#{pane_current_path}"

unbind '"'
bind - split-window -v

bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5

bind -r m resize-pane -Z

# popup cheatsheet
bind ? display-popup -E -w 50% -h 80% "cat ~/.config/tmux/cheatsheet.txt && read"

# popup shell in current directory
bind S display-popup -E -w 80% -h 80% -d "#{pane_current_path}"

# popup lazygit
bind g display-popup -E -w 90% -h 90% -d "#{pane_current_path}" "lazygit"

# sesh: fuzzy session switcher (zoxide + tmux sessions + config templates)
bind f run-shell "sesh connect \"$(
  sesh list -i | fzf --tmux 80%,50% \
    --no-sort --ansi \
    --prompt '  session: ' \
    --preview-window 'right:40%:wrap' \
    --bind 'tab:down,btab:up'
)\""

# sesh: toggle last session
bind a run-shell "sesh last"

set -g mouse on

# Mouse scroll behavior:
# - alternate screen (vim, claude, etc): pass wheel events to the app
# - normal pane: scroll history; auto-exit copy mode when reaching bottom
bind -n WheelUpPane \
  if-shell -F '#{alternate_on}' \
    'send-keys -M' \
    'if-shell -F "#{pane_in_mode}" "send-keys -M" "copy-mode -e"'
bind -n WheelDownPane \
  if-shell -F '#{alternate_on}' \
    'send-keys -M' \
    'if-shell -F "#{pane_in_mode}" "send-keys -M" ""'
bind -T copy-mode-vi WheelUpPane   send -X scroll-up
bind -T copy-mode-vi WheelDownPane send -X scroll-down

set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection # start selecting text with "v"
bind-key -T copy-mode-vi 'y' send -X copy-selection # copy text with "y"

unbind -T copy-mode-vi MouseDragEnd1Pane # don't exit copy mode after dragging with mouse

run '~/.config/tmux/plugins/tpm/tpm'
